# Secret scanning before every commit (Constitution, Principle I).
# Setup after cloning: `pre-commit install`
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
