# Local development profile: seed data, Swagger UI, cookie without Secure (plain http://localhost).
spring:
  flyway:
    locations: classpath:db/migration,classpath:db/seed

springdoc:
  swagger-ui:
    enabled: true

shop:
  app:
    cookie:
      secure: false
