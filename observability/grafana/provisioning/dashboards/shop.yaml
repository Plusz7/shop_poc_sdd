# File provider for the shop dashboards (FR-033): the JSON files in the repository are the only source.
apiVersion: 1

providers:
  - name: shop
    folder: Shop
    type: file
    disableDeletion: true
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
